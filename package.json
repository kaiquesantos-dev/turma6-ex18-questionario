{
  "name": "turma-ex18-questionario",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Cadastro de Questionários (API com json-server)",
  "scripts": {
    "server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
